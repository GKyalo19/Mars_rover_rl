# Scripts (coming in later chapters)

| Script | Chapter | Purpose |
|--------|---------|---------|
| `zero_agent.py` | 4–6 | Step the env with zero actions (wiring test) |
| `train.py` | 7 | Run RSL-RL PPO training |
| `play.py` | 9 | Load a checkpoint and watch the policy |

Do not implement these until the matching chapter — empty folder is OK for now.
